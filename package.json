{
  "name": "ses-to-documentdb-lambdas",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "tsc ",
    "build:watch": "tsc -w ",
    "package": "zip -r lambda.zip . -x '*.git*' -x '*.zip'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan Rumney <dan.rumney@ahanapediatrics.com>",
  "dependencies": {
    "@types/mongodb": "^3.6.12",
    "aws-lambda": "^1.0.5",
    "mongodb": "^3.5.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.46"
  }
}
